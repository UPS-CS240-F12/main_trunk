using UnityEngine;
using System.Collections;

public class TileMessenger : MonoBehaviour {
	
	public Vector3 message;
	
	// Use this for initialization
	void Start () {
		message = new Vector3(0,0,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
